# 摄入趋势图表功能说明

## 功能概述

在历史记录页面的统计卡片和记录列表之间新增了摄入趋势折线图，可以直观地查看不同时间范围内的水分摄入趋势。

## 功能特性

### 1. 时间范围切换

支持三个时间维度查看：
- **周**：显示近7天的摄入趋势
- **月**：显示近30天的摄入趋势
- **年**：显示近365天的摄入趋势

### 2. 图表样式

- **折线图**：使用平滑的折线展示数据趋势
- **渐变填充**：折线下方有渐变色填充区域，更美观
- **数据点**：每个数据点都有圆点标记
- **网格背景**：浅色网格线辅助查看
- **坐标轴标签**：
  - Y轴：显示最大值和0值
  - X轴：显示日期（格式：月/日）

### 3. 数据处理

- 自动填充空白日期（没有记录的日期显示为0）
- 根据时间范围动态加载数据
- 优化了数据库查询，分别加载列表数据和图表数据

## 技术实现

### 新增文件

**utils/chart.js** - 图表绘制工具模块
```javascript
// 主要导出函数
drawLineChart(ctx, config)     // 绘制折线图
prepareChartData(records, days) // 准备图表数据
```

### 核心功能

1. **Canvas 绘图**：使用微信小程序原生 Canvas API
2. **轻量级实现**：无需引入第三方图表库
3. **响应式设计**：自动适配画布大小
4. **性能优化**：延迟初始化画布，避免加载问题

### 修改的文件

**pages/history/history.wxml**
- 添加图表卡片区域
- 添加时间范围切换标签

**pages/history/history.js**
- 引入图表工具模块
- 添加 `chartRange` 状态（默认7天）
- 添加 `allRecords` 数组存储一年数据
- 实现 `initCanvas()` 初始化画布
- 实现 `drawChart()` 绘制图表
- 实现 `switchChartRange()` 切换时间范围
- 优化 `loadRecords()` 分别加载列表和图表数据

**pages/history/history.wxss**
- 添加 `.chart-card` 样式
- 添加 `.chart-header` 样式
- 添加 `.chart-tabs` 和 `.chart-tab` 样式
- 添加 `.chart-canvas` 样式

## 使用方法

1. 进入"我的"页面，登录后点击"历史记录"
2. 在统计卡片下方即可看到摄入趋势图表
3. 点击"周"、"月"、"年"标签切换查看不同时间范围
4. 图表会自动显示对应时间范围内的摄入数据

## 数据说明

- **有记录的日期**：显示实际摄入量（单位：ml）
- **无记录的日期**：显示为0
- **Y轴范围**：自动根据最大值调整
- **X轴标签**：根据数据量自动调整显示密度

## 优势

✅ **轻量级**：使用原生 Canvas，无需额外依赖
✅ **高性能**：优化的绘图算法，流畅无卡顿
✅ **美观度**：渐变色填充，现代化设计风格
✅ **易维护**：模块化设计，代码清晰
✅ **可扩展**：可轻松添加其他图表类型

## 示例效果

```
摄入趋势
[周] [月] [年]  ← 切换标签

   1200ml ─────────────
          ╱╲    ╱╲
   600ml ╱  ╲  ╱  ╲
        ╱    ╲╱    ╲
   0ml ─────────────────
       1/8  1/10  1/12  ← 日期标签
```

## 未来优化方向

1. 添加数据点点击事件，显示详细信息
2. 支持柱状图、饼图等其他图表类型
3. 添加图表数据导出功能
4. 支持自定义时间范围选择
5. 添加图表缩放、拖动交互

---

**Created**: 2025-11-15
**Version**: 1.0.0
