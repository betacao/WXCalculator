# 快速开始指南

## 前提条件

- 已注册微信小程序账号
- 已安装微信开发者工具
- 已开通云开发服务

## 配置步骤

### 第一步：克隆并打开项目

1. 克隆或下载本项目到本地
2. 使用微信开发者工具打开项目目录
3. 使用你的小程序AppID

### 第二步：开通云开发

1. 在微信开发者工具中点击"云开发"按钮
2. 按照提示开通云开发服务（选择按量付费即可，有免费额度）
3. 创建一个云环境，记录环境ID（如：cloud1-xxxxx）

### 第三步：更新云环境ID

打开 `pages/index/index.js`，修改第6行的环境ID：

```javascript
const CONFIG = {
  CLOUD_ENV: 'cloud1-8galpdysfe64e2a7',  // 改为你的环境ID
  // ...
};
```

### 第四步：创建数据库集合

1. 在微信开发者工具的"云开发"控制台中，进入"数据库"
2. 点击"添加集合"
3. 输入集合名称：`water_records`
4. 权限设置选择："仅创建者可读写"
5. 点击确定

### 第五步：部署云函数

1. 在微信开发者工具左侧，找到 `cloudfunctions/login` 目录
2. 右键点击该目录
3. 选择"上传并部署：云端安装依赖"
4. 等待部署完成（约1-2分钟）

### 第六步：准备TabBar图标

由于项目需要TabBar图标，你有两个选择：

**方案A：使用图标（推荐）**

在 `images/` 目录下放置以下图标文件：
- `calculator.png` - 计算器图标（未选中）
- `calculator-active.png` - 计算器图标（选中）
- `profile.png` - 个人中心图标（未选中）
- `profile-active.png` - 个人中心图标（选中）
- `login.png` - 登录页展示图标

尺寸：81px × 81px，PNG格式

可从 [iconfont](https://www.iconfont.cn/) 下载相关图标。

**方案B：暂时移除TabBar**

如果暂时没有图标，可以临时注释掉 `app.json` 中的 tabBar 配置：

```json
{
  "pages": [...],
  "window": {...},
  // "tabBar": {...},  // 临时注释
  "sitemapLocation": "sitemap.json"
}
```

注释后需要手动导航到个人中心页面。

### 第七步：运行项目

1. 点击"编译"按钮
2. 如果是真机调试，点击"预览"生成二维码
3. 测试基本功能是否正常

---

## 功能测试清单

### 1. 测试基础计算功能

- [ ] 搜索食物
- [ ] 切换分类
- [ ] 选择食物并设置重量
- [ ] 计算总含水量
- [ ] 查看已选食物列表

### 2. 测试登录功能

- [ ] 点击TabBar切换到"我的"页面
- [ ] 点击"微信登录"按钮
- [ ] 授权并完成登录
- [ ] 查看统计数据是否显示

### 3. 测试保存记录功能

- [ ] 登录后返回计算器页面
- [ ] 选择食物并计算
- [ ] 点击"保存记录"
- [ ] 检查是否保存成功

### 4. 测试历史记录功能

- [ ] 在"我的"页面点击"历史记录"
- [ ] 查看记录列表
- [ ] 点击记录查看详情
- [ ] 左滑删除某条记录
- [ ] 查看统计卡片数据

---

## 常见问题解决

### Q1: 云函数调用失败

**错误信息**: `cloud.callFunction:fail`

**解决方案**:
1. 检查云函数是否部署成功
2. 在云开发控制台→云函数中查看login函数状态
3. 重新上传并部署云函数
4. 确认项目关联的云环境ID正确

### Q2: 数据库操作失败

**错误信息**: `collection(...).add is not a function`

**解决方案**:
1. 确认已开通云开发
2. 确认数据库集合 `water_records` 已创建
3. 检查 `wx.cloud.init()` 是否正确初始化

### Q3: TabBar图标不显示

**错误信息**: 页面显示空白或报错

**解决方案**:
1. 确认图标文件已放置在 `images/` 目录
2. 检查图标文件名是否与 `app.json` 中的配置一致
3. 确认图标格式为PNG且尺寸合适
4. 或临时注释掉tabBar配置

### Q4: 登录后无法保存数据

**错误信息**: `errCode: -502003`

**解决方案**:
1. 检查数据库权限设置
2. 确认用户已成功登录（有openid）
3. 在云开发控制台→数据库中手动查看权限规则

### Q5: 页面跳转失败

**错误信息**: `navigateTo/switchTab fail`

**解决方案**:
1. TabBar页面必须使用 `wx.switchTab`
2. 普通页面使用 `wx.navigateTo`
3. 检查页面路径是否正确

---

## 云开发配置检查

打开微信开发者工具的"云开发"控制台，确认：

### 数据库
- ✅ 集合 `water_records` 已创建
- ✅ 权限设置为"仅创建者可读写"

### 云函数
- ✅ `login` 函数显示"部署成功"
- ✅ 函数版本为最新

### 环境配置
- ✅ 环境ID与代码中的配置一致
- ✅ 环境状态正常

---

## 开发建议

### 调试技巧

1. **打开调试模式**: 在 `project.config.json` 中：
```json
{
  "setting": {
    "checkSiteMap": false,
    "es6": true,
    "minified": false
  }
}
```

2. **查看云函数日志**:
   - 云开发控制台 → 云函数 → login → 日志
   - 可以看到每次调用的详细信息

3. **查看数据库数据**:
   - 云开发控制台 → 数据库 → water_records
   - 可以直接查看和编辑数据

### 本地调试

建议使用真机调试测试登录功能，因为：
- 模拟器可能无法正确获取用户信息
- 真机调试更接近真实使用场景

### 数据安全

- 不要在代码中硬编码敏感信息
- 定期检查数据库权限设置
- 注意用户隐私保护

---

## 下一步

功能正常后，可以考虑：

1. **优化界面**: 美化UI，添加动画效果
2. **功能扩展**: 参考 `FEATURES.md` 中的建议
3. **性能优化**: 添加更多缓存机制
4. **数据分析**: 添加图表展示
5. **发布上线**: 提交审核并发布

---

## 获取帮助

- 查看 `DATABASE_SETUP.md` 了解数据库详细配置
- 查看 `FEATURES.md` 了解功能详细说明
- 查看微信官方文档：https://developers.weixin.qq.com/miniprogram/dev/wxcloud/basis/getting-started.html

## 注意事项

1. **首次运行**: 首次运行可能需要编译npm，点击"工具" → "构建npm"
2. **真机调试**: 登录功能建议使用真机调试
3. **云开发额度**: 注意免费额度限制，个人使用足够
4. **版本要求**: 微信基础库版本 ≥ 2.10.0

祝你开发顺利！🎉
